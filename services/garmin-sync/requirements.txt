# Pinned so a Garmin-side TLS/fingerprint change can't silently pull an
# incompatible release. python-garminconnect >=0.3.5 rebuilt login on curl_cffi
# (survives the March 2026 Cloudflare fingerprinting break); bump deliberately.
garminconnect==0.3.6
# >=2.32.4 fixes CVE-2024-47081 (.netrc credential leak via a crafted URL).
requests==2.32.4
